name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    branches:
      - staging
    types:
      - completed

jobs:
  deploy:
    runs-on: [self-hosted, ovh-vps]

    steps:
      - name: Pull Docker image
        run: sudo docker pull ${{ secrets.DOCKER_USERNAME }}/like-minded-nus-staging:latest
      - name: Delete Old Docker container
        run: sudo docker rm -f like-minded-nus-staging-container || true
      - name: Run Docker Container
        run: sudo docker run -d -p 8080:8080 --name like-minded-nus-staging-container ${{ secrets.DOCKER_USERNAME }}/like-minded-nus-staging:latest